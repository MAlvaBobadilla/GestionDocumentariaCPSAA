<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GestiónDocumentariaCPSAA" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@android:color/background_dark</item>
    </style>

    <style name="Theme.GestiónDocumentariaCPSAA" parent="Base.Theme.GestiónDocumentariaCPSAA" />
    <style name="Boton">
        <item name="android:backgroundTint">@color/red</item>
    </style>
    <style name="TextoBoton" parent="Boton">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>